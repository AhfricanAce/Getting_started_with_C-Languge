#include <stdio.h>

int main() {
        int age; // declaration of an integer variable named 'age'
        
        age = 25; // assigning a value to the 'age' variable
        
        printf("My age is %d\n", age); // printing the value of the 'age' variable
        
        return 0;
}
